# Engineering v2.2.6 audit10 semantic-repair plan

## Boundaries

Work only in the fresh paired audit10 worktrees based on the rejected audit9
commits. Preserve every earlier commit, receipt, test log, audit verdict,
installed v2.2.5, and rollback artifact. Do not publish, merge, install,
activate, modify README pull request #3, or bind populated later outcomes into
candidate Git.

## Requirement-to-test order

1. Add repository regressions proving that the host-approved ledger contains
   an independently supplied, source-excerpt-bound requirement projection;
   candidate registry omission, source conflict, or a candidate-generated
   projection fails closed.
2. Add README regressions requiring the native-observability disclosure and
   its truthful capability matrix before any LangGraph or Langfuse comparison,
   including explicit negative capability and authority boundaries.
3. Add controller regressions requiring one complete design-and-evidence map
   for every active later outcome before dependent dispatch. Missing,
   duplicate, broad-scope-only, Unknown, proxy, or wrong-artifact mappings fail
   closed; exact complete mappings admit idempotently.
4. Implement each correction only after its focused regression is red, then
   rerun the focused and affected suites.
5. Incorporate any final technical audit9 findings before candidate freeze.
6. Run the canonical internal suite, deterministic public export, canonical
   public suite, repository, sensitivity, compilation, parity, cleanliness,
   and object-integrity gates serially on the exact final pair.
7. Generate immutable execution, incident, matrix, and paired-freeze receipts;
   stop before delivery so fresh independent auditors can inspect the pair.

## Freeze conditions

The pair cannot freeze until the external owner-source record is host-issued
outside candidate Git, every active later outcome has complete row-level
dispatch evidence, all audit9 findings are reconciled, and all exact-artifact
gates pass. Public metadata assurance remains delivery-only Unknown.
